# Claude Integration

Repository instructions are provided through `../../CLAUDE.md`, while the canonical Engineering Focus contract remains `../../skills/engineering-focus/SKILL.md`.

Default mode is `BALANCED`.

Use `STRICT` for bounded implementation/validation work and `EXPLORATORY` deliberately for alternatives or adversarial ideation. Exploratory findings should pass through focused validation before being treated as verified.

## Status

The repository instruction adapter exists. Additional Claude-specific plugin/skill packaging should be added only after its target runtime format is verified and can be tested without duplicating the canonical contract.
