# Engineering Focus — DISABLED

Do not apply Engineering Focus optimization behavior for this run.

Follow the host system, project, and user instructions normally.

`DISABLED` exists for:

- compatibility;
- controlled experiments;
- benchmarking against an unguided/default baseline;
- task classes where Engineering Focus has been measured to reduce quality;
- explicit user/operator choice.

If telemetry is being collected, record the policy mode as `DISABLED` so the run can be compared fairly with Engineering Focus-enabled runs.
